# Just a fella :)
if (self.name() == 'Creepie1') {
	# Say hi!
	#log 'Hello!'
	self.fly_away()
} else {
	if (self.name() == 'Creepie2') {
		# This shouldn't happen :(
		#log 'uh oh'
		whyman = 'uwu'
		whymans_broken_hand = (whyman.punch_thing() + 50)
		exit F
	} else {
		name = self.name()
		#log name
		exit R
	}
}

# We done :)
exit S
